//	animate() 함수는 여러 css 스타일을 이용해서 사용자 이펙트를 정의할 수 있다.
//	$('선택자').animate(
//		{스타일}		// 이펙트를 구성할 css 스타일 속성
//		[, 재생시간]
//		[, '속도함수']
//		[, 콜백함수()]
//	);

$(() => {
	$('h2').click(function () {
		$('div > p').show();
		$('p').each(function(index) {
			$(this).animate();
		});
	});
});